# This repository has been merged

**Moved to `pai-list/pai-gateways`** (unified Cloudflare Workers edge: api-gateway + mcp).

- Spec & code: `src/gateway/` (api-gateway) or `src/mcp/` (mcp)
- Install: `npm ci && npm run dev:gateway` or `npm run dev:mcp`

This repo is archived and read-only.
