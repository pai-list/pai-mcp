name: Reusable Node/TypeScript CI
description: Universal CI for PAI Node/TS repos (monorepo or single package)
on:
  workflow_call:
    inputs:
      turbo-targets:
        description: "Turbo targets to run (space-separated)"
        required: false
        type: string
        default: "build test lint typecheck"
      node-version:
        description: "Node.js version"
        required: false
        type: string
        default: "22"
      pnpm-version:
        description: "pnpm version"
        required: false
        type: string
        default: "9"
      working-directory:
        description: "Working directory (relative to repo root)"
        required: false
        type: string
        default: "."
      install-frozen:
        description: "Use frozen lockfile"
        required: false
        type: boolean
        default: true
      cache-key-suffix:
        description: "Suffix for cache key"
        required: false
        type: string
        default: ""
    secrets:
      NPM_TOKEN:
        required: false
      GH_TOKEN:
        required: false

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  node-ci:
    name: Node CI (${{ inputs.working-directory }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'npm'
          cache-dependency-path: |
            ${{ inputs.working-directory }}/pnpm-lock.yaml
            ${{ inputs.working-directory }}/package-lock.json
      
      - name: Install dependencies
        id: install
        run: |
          cd ${{ inputs.working-directory }}
          if [ -f pnpm-lock.yaml ]; then
            echo "manager=pnpm" >> "$GITHUB_OUTPUT"
            npm i -g pnpm@${{ inputs.pnpm-version }} --silent
            if [[ "${{ inputs.install-frozen }}" == "true" ]]; then
              pnpm install --frozen-lockfile
            else
              pnpm install
            fi
          else
            echo "manager=npm" >> "$GITHUB_OUTPUT"
            if [[ "${{ inputs.install-frozen }}" == "true" ]]; then
              npm ci
            else
              npm install
            fi
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      
      - name: Typecheck
        if: contains(inputs.turbo-targets, 'typecheck')
        run: ${{ steps.install.outputs.manager }} run typecheck 2>&1 || (${{ steps.install.outputs.manager }} run tsc --noEmit 2>&1 || true)
      
      - name: Lint
        if: contains(inputs.turbo-targets, 'lint')
        run: ${{ steps.install.outputs.manager }} run lint 2>&1 || true
      
      - name: Build
        if: contains(inputs.turbo-targets, 'build')
        run: ${{ steps.install.outputs.manager }} run build
        env:
          DATABASE_URL: "postgresql://localhost:5432/dummy"
      
      - name: Test
        if: contains(inputs.turbo-targets, 'test')
        run: ${{ steps.install.outputs.manager }} test -- --ci --runInBand 2>&1 || true
      
      - name: Dead code check (knip)
        if: contains(inputs.turbo-targets, 'build')
        run: npx knip --no-progress 2>&1 || true
        continue-on-error: true
      
      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ inputs.working-directory }}-${{ github.sha }}
          path: |
            ${{ inputs.working-directory }}/dist/
            ${{ inputs.working-directory }}/.next/
            ${{ inputs.working-directory }}/build/
          retention-days: 7